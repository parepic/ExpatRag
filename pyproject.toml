[project]
name = "ExpatRag"
version = "0.1.0"
requires-python = ">=3.12,<4.0"

[tool.uv.workspace]
members = ["backend", "data_pipeline"]

[tool.setuptools]
packages = []


[tool.poetry.dependencies]
langchain-tavily = "^0.2.18"
[dependency-groups]
dev = []
